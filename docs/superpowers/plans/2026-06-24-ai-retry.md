# AI 大模型调用重试机制 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为所有 AI 大模型调用增加可配置次数的同接口重试，仅对可重试错误（网络/超时/429/5xx）做指数退避重试，重试耗尽后走现有 fallback；默认关闭（0 次）。

**Architecture:** 新增 `lib/ai/retry.js`（`isRetryableError` + `withRetry`），在四个调用点（`openaiChat.callAi`、`aiCaller.createDirectCompletion`、`aiCaller.createToolCompletion`、`imageProcessor` 6 个底层方法的 `axios.post`）用 `withRetry` 包裹「单次请求」。重试在主接口请求层，fallback 在上层，`attemptedFallback` 守卫不受影响。配置项 `ai.retryCount` 默认 0，经 guoba schema 暴露到网页控制台（前端零改动），纳入 `getConfigSignature` 支持热生效。

**Tech Stack:** Node.js ESM、OpenAI SDK v4、axios、Yunzai-Bot 插件体系。测试用 Node 内置 `node:test` + `node:assert`（无新依赖）。

**Spec:** `docs/superpowers/specs/2026-06-24-ai-retry-design.md`

---

## 文件结构

| 文件 | 责任 | 操作 |
|---|---|---|
| `lib/ai/retry.js` | 通用重试工具：`isRetryableError` 判定、`withRetry` 包裹器 | 新建 |
| `lib/ai/retry.test.mjs` | retry 模块单元测试 | 新建 |
| `modules/openai/openaiChat.js` | 文本/多模态调用：`init` 收 retryCount、`callAi` 内 `create` 包裹 | 修改 |
| `lib/ai/aiCaller.js` | direct/tool 调用包裹；`init`/`refreshRuntimeConfig` 传 retryCount；`getConfigSignature` 加 retryCount | 修改 |
| `lib/ai/imageProcessor.js` | `mergeImageConfig` 继承 retryCount；6 个底层方法 `axios.post` 包裹 | 修改 |
| `config/ai.json` | 新增 `retryCount` 默认值 0 | 修改 |
| `guoba/configSchema.js` | 新增 `ai.retryCount` 设置项 | 修改 |
| `guoba/configHandler.js` | `case 'ai'` 加 retryCount 范围校验 | 修改 |

---

## Task 1: 重试工具模块 + 测试（TDD）

**Files:**
- Create: `lib/ai/retry.js`
- Test: `lib/ai/retry.test.mjs`

这是整个功能的核心，独立可测，先 TDD 打好底。

- [ ] **Step 1: 写失败测试**

创建 `lib/ai/retry.test.mjs`：

```js
import { test, assert } from 'node:test';
import { isRetryableError, withRetry } from './retry.js';

// ---- isRetryableError ----

test('isRetryableError: 超时类错误可重试', () => {
  assert.equal(isRetryableError(new Error('AI请求超时（60000ms）')), true);
  assert.equal(isRetryableError(Object.assign(new Error('timeout'), { code: 'ETIMEDOUT' })), true);
  assert.equal(isRetryableError(Object.assign(new Error('aborted'), { code: 'ECONNABORTED' })), true);
  assert.equal(isRetryableError(Object.assign(new Error('reset'), { code: 'ECONNRESET' })), true);
});

test('isRetryableError: 429 / 5xx 可重试', () => {
  assert.equal(isRetryableError(Object.assign(new Error('rate limited'), { status: 429 })), true);
  assert.equal(isRetryableError(Object.assign(new Error('server err'), { status: 500 })), true);
  assert.equal(isRetryableError(Object.assign(new Error('bad gw'), { status: 502 })), true);
  // axios 风格：error.response.status
  assert.equal(isRetryableError(Object.assign(new Error('x'), { response: { status: 503 } })), true);
});

test('isRetryableError: 4xx 鉴权/参数错误不可重试', () => {
  assert.equal(isRetryableError(Object.assign(new Error('unauthorized'), { status: 401 })), false);
  assert.equal(isRetryableError(Object.assign(new Error('forbidden'), { status: 403 })), false);
  assert.equal(isRetryableError(Object.assign(new Error('bad request'), { status: 400 })), false);
  assert.equal(isRetryableError(Object.assign(new Error('x'), { response: { status: 404 } })), false);
});

test('isRetryableError: 未知普通错误不可重试', () => {
  assert.equal(isRetryableError(new Error('something weird')), false);
});

// ---- withRetry ----

test('withRetry: retries=0 直接执行不重试', async () => {
  let calls = 0;
  const fn = async () => { calls++; return 'ok'; };
  const result = await withRetry(fn, { retries: 0 });
  assert.equal(result, 'ok');
  assert.equal(calls, 1);
});

test('withRetry: 首次成功不重试', async () => {
  let calls = 0;
  const fn = async () => { calls++; return 'ok'; };
  const result = await withRetry(fn, { retries: 3, baseDelay: 1, sleep: async () => {} });
  assert.equal(result, 'ok');
  assert.equal(calls, 1);
});

test('withRetry: 可重试错误重试到成功', async () => {
  let calls = 0;
  const fn = async () => {
    calls++;
    if (calls < 3) throw Object.assign(new Error('timeout'), { code: 'ETIMEDOUT' });
    return 'ok';
  };
  const result = await withRetry(fn, { retries: 3, baseDelay: 1, sleep: async () => {} });
  assert.equal(result, 'ok');
  assert.equal(calls, 3);
});

test('withRetry: 不可重试错误立即抛出不重试', async () => {
  let calls = 0;
  const fn = async () => {
    calls++;
    throw Object.assign(new Error('unauthorized'), { status: 401 });
  };
  await assert.rejects(() => withRetry(fn, { retries: 3, baseDelay: 1, sleep: async () => {} }), /unauthorized/);
  assert.equal(calls, 1);
});

test('withRetry: 重试耗尽抛出最后的错误', async () => {
  let calls = 0;
  const fn = async () => {
    calls++;
    throw Object.assign(new Error('server error'), { status: 500 });
  };
  await assert.rejects(() => withRetry(fn, { retries: 2, baseDelay: 1, sleep: async () => {} }), /server error/);
  assert.equal(calls, 3); // 首次 + 2 次重试
});

test('withRetry: onRetry 回调被调用且携带尝试信息', async () => {
  const retries = [];
  let calls = 0;
  const fn = async () => {
    calls++;
    if (calls < 2) throw Object.assign(new Error('rate'), { status: 429 });
    return 'ok';
  };
  await withRetry(fn, {
    retries: 3, baseDelay: 1, sleep: async () => {},
    onRetry: (info) => retries.push(info),
  });
  assert.equal(retries.length, 1);
  assert.equal(retries[0].attempt, 1);
  assert.equal(retries[0].delay, 1);
  assert.match(String(retries[0].error?.message || retries[0].error), /rate/);
});

test('withRetry: 退避上限不超过 maxDelay', async () => {
  const delays = [];
  let calls = 0;
  const fn = async () => { calls++; throw Object.assign(new Error('x'), { status: 500 }); };
  await withRetry(fn, {
    retries: 5, baseDelay: 1000, maxDelay: 4000,
    sleep: async (ms) => delays.push(ms),
    onRetry: () => {},
  }).catch(() => {});
  // 退避序列：1000, 2000, 4000, 4000(cap), 4000(cap)
  assert.deepEqual(delays, [1000, 2000, 4000, 4000, 4000]);
});

test('withRetry: isFailureResult 触发重试（返回失败结果而非抛异常）', async () => {
  let calls = 0;
  const fn = async () => {
    calls++;
    if (calls < 2) return { success: false, error: 'fail' };
    return { success: true };
  };
  const result = await withRetry(fn, {
    retries: 3, baseDelay: 1, sleep: async () => {},
    isFailureResult: (r) => r && r.success === false,
  });
  assert.equal(result.success, true);
  assert.equal(calls, 2);
});
```

- [ ] **Step 2: 运行测试验证失败**

Run: `node --test lib/ai/retry.test.mjs`
Expected: FAIL — `Cannot find module './retry.js'`

- [ ] **Step 3: 实现 retry 模块**

创建 `lib/ai/retry.js`：

```js
/**
 * AI 接口调用重试工具。
 * 仅对「可重试错误」（网络错误/超时/429/5xx）做指数退避重试；
 * 4xx 鉴权/参数错误不重试，避免对永久性错误浪费配额与时间。
 */

/** 网络层错误码，命中即视为可重试 */
const RETRYABLE_NETWORK_CODES = new Set([
  'ETIMEDOUT',
  'ECONNABORTED',
  'ECONNRESET',
  'ENOTFOUND',
  'EAI_AGAIN',
  'ECONNREFUSED',
  'EPIPE',
  'EHOSTUNREACH',
  'ENETUNREACH',
]);

/**
 * 判断错误是否值得重试。
 * @param {Error|any} error
 * @returns {boolean}
 */
export function isRetryableError(error) {
  if (!error) return false;

  const code = String(error?.code || '').trim();
  const name = String(error?.name || '').trim();
  const message = String(error?.message || error || '').trim();
  const combined = `${name} ${code} ${message}`;

  // 1. 网络层错误码
  if (RETRYABLE_NETWORK_CODES.has(code)) return true;

  // 2. 超时类（消息匹配）
  if (/超时|timeout|timed out|time out|abort/i.test(combined)) return true;

  // 3. HTTP 状态码：429 / 5xx 可重试，4xx 不可重试
  const status = Number(error?.status || error?.response?.status || 0);
  if (status === 429 || (status >= 500 && status <= 599)) return true;

  return false;
}

/**
 * 计算第 attempt 次重试（从 1 开始）的退避间隔。
 * 指数退避：baseDelay * 2^(attempt-1)，封顶于 maxDelay。
 */
function computeBackoff(attempt, baseDelay, maxDelay) {
  const raw = baseDelay * Math.pow(2, attempt - 1);
  return Math.min(raw, maxDelay);
}

/**
 * 通用重试包裹器。
 *
 * @param {() => Promise<any>} fn 要执行的异步操作
 * @param {object} options
 * @param {number} [options.retries=0] 重试次数（不含首次），0 即不重试
 * @param {(result:any)=>boolean|null} [options.isFailureResult=null] 可选，用于「返回失败结果而非抛异常」型调用；返回 true 表示需重试
 * @param {number} [options.baseDelay=500] 初始退避间隔 ms
 * @param {number} [options.maxDelay=8000] 退避上限 ms
 * @param {(ms:number)=>Promise<void>} [options.sleep] 休眠函数（可注入便于测试）
 * @param {(info:{attempt:number,delay:number,error:any})=>void} [options.onRetry=null] 重试回调
 * @returns {Promise<any>}
 */
export async function withRetry(fn, {
  retries = 0,
  isFailureResult = null,
  baseDelay = 500,
  maxDelay = 8000,
  sleep = (ms) => new Promise((resolve) => setTimeout(resolve, ms)),
  onRetry = null,
} = {}) {
  // 0 次重试：直接执行，零开销
  const maxRetries = Number(retries) > 0 ? Number(retries) : 0;
  if (maxRetries <= 0) return fn();

  const isResolveFailure = (result) =>
    typeof isFailureResult === 'function' && isFailureResult(result) === true;

  let lastError = null;
  let lastResult;
  let threw = false;

  // 首次执行
  try {
    lastResult = await fn();
    if (!isResolveFailure(lastResult)) return lastResult;
    threw = false;
  } catch (error) {
    // 不可重试错误立即抛出
    if (!isRetryableError(error)) throw error;
    lastError = error;
    threw = true;
  }

  // 重试循环
  for (let attempt = 1; attempt <= maxRetries; attempt++) {
    const delay = computeBackoff(attempt, baseDelay, maxDelay);
    if (typeof onRetry === 'function') {
      onRetry({ attempt, delay, error: threw ? lastError : null });
    }
    await sleep(delay);
    try {
      lastResult = await fn();
      threw = false;
      lastError = null;
      if (!isResolveFailure(lastResult)) return lastResult;
    } catch (error) {
      // 不可重试错误立即抛出
      if (!isRetryableError(error)) throw error;
      lastError = error;
      threw = true;
    }
  }

  // 重试耗尽
  if (threw) throw lastError;
  return lastResult;
}
```

- [ ] **Step 4: 运行测试验证通过**

Run: `node --test lib/ai/retry.test.mjs`
Expected: PASS（全部测试通过）

- [ ] **Step 5: 提交**

```bash
git add lib/ai/retry.js lib/ai/retry.test.mjs
git commit -m "feat(ai): add withRetry/isRetryableError retry utility with tests"
```

---

## Task 2: openaiChat 接入重试（文本/多模态路径）

**Files:**
- Modify: `modules/openai/openaiChat.js`

`openaiChat` 是底层 OpenAI 调用类，`callAi` 内的 `this.openai.chat.completions.create` 是真正会因网络/超时失败的地方。需让 `init` 接收 `retryCount`，并在 `callAi` 内用 `withRetry` 包裹 `create` 调用。

- [ ] **Step 1: 修改 init 签名与构造函数**

`modules/openai/openaiChat.js` 第 35-52 行。把构造函数加 `this.retryCount = 0;`，`init` 增加第四个参数 `retryCount`：

旧代码（第 35-52 行）：
```js
class OpenaiChat {
  constructor() {
    this.openai = null;
    this.timeout = 60000;
  }

  /**
   * @param apiKey 密钥
   * @param baseUrl openaiAPI地址
   */
  init(apiKey, baseUrl, timeout = 60000) {
    this.timeout = Number(timeout) > 0 ? Number(timeout) : 60000;
    this.openai = new OpenAI({
      apiKey: apiKey,
      baseURL: baseUrl,
      timeout: this.timeout,
    });
  }
```

新代码：
```js
class OpenaiChat {
  constructor() {
    this.openai = null;
    this.timeout = 60000;
    this.retryCount = 0;
  }

  /**
   * @param apiKey 密钥
   * @param baseUrl openaiAPI地址
   * @param timeout 超时时间(毫秒)
   * @param retryCount 接口失败重试次数，0 表示不重试
   */
  init(apiKey, baseUrl, timeout = 60000, retryCount = 0) {
    this.timeout = Number(timeout) > 0 ? Number(timeout) : 60000;
    this.retryCount = Number(retryCount) > 0 ? Number(retryCount) : 0;
    this.openai = new OpenAI({
      apiKey: apiKey,
      baseURL: baseUrl,
      timeout: this.timeout,
    });
  }
```

- [ ] **Step 2: 顶部导入 withRetry**

`modules/openai/openaiChat.js` 第 2 行后追加导入。旧：
```js
import { logAiUsage } from '../../lib/ai/usageLogger.js';
```
新：
```js
import { logAiUsage } from '../../lib/ai/usageLogger.js';
import { withRetry } from '../../lib/ai/retry.js';
```

- [ ] **Step 3: 用 withRetry 包裹 callAi 内的 create 调用**

`modules/openai/openaiChat.js` 第 94-101 行。旧：
```js
      const completion = await this.openai.chat.completions.create({
        messages: sanitizeMessages(finalMessages),
        model: model,
        temperature: temperature,
        frequency_penalty: 0.2,
        presence_penalty: 0.2,
        stream:false
      });
```
新：
```js
      const completion = await withRetry(
        () => this.openai.chat.completions.create({
          messages: sanitizeMessages(finalMessages),
          model: model,
          temperature: temperature,
          frequency_penalty: 0.2,
          presence_penalty: 0.2,
          stream: false,
        }),
        {
          retries: this.retryCount,
          onRetry: (info) => logger.warn(`[crystelf-ai] AI请求重试 ${info.attempt}/${this.retryCount}，${info.delay}ms 后重试: ${info.error?.message || info.error}`),
        },
      );
```

注意：`callAi` 的 catch 块（第 161 行起）的 `normalizeAiErrorMessage`/`logAiUsage`/`return { success:false, error }` 逻辑**保持不变**。重试耗尽后异常会冒到 catch，由现有错误处理统一兜底。

- [ ] **Step 4: 语法校验**

Run: `node --check modules/openai/openaiChat.js`
Expected: 无输出（语法正确）。如报 `ReferenceError: logger is not defined` 属正常——`--check` 只校验语法不执行，logger 是运行时全局注入的。只要无语法错误即可。

- [ ] **Step 5: 提交**

```bash
git add modules/openai/openaiChat.js
git commit -m "feat(ai): wrap openaiChat.callAi create with withRetry"
```

---

## Task 3: aiCaller 接入重试 + 传递 retryCount + 热生效签名

**Files:**
- Modify: `lib/ai/aiCaller.js`

三处改动：(a) 顶部导入 `withRetry`；(b) `createDirectCompletion`/`createToolCompletion` 用 `withRetry` 包裹；(c) `init`/`refreshRuntimeConfig`/`getOpenaiInstanceForConfig` 调用 `openaiChat.init` 时传入 `retryCount`；(d) `getConfigSignature` 加 `retryCount`。

- [ ] **Step 1: 顶部导入 withRetry**

`lib/ai/aiCaller.js` 第 8 行后追加。旧：
```js
import { buildAiFallbackConfig, buildImageFallbackConfig, shouldRetryWithFallback } from './apiFallback.js';
```
新：
```js
import { buildAiFallbackConfig, buildImageFallbackConfig, shouldRetryWithFallback } from './apiFallback.js';
import { withRetry } from './retry.js';
```

- [ ] **Step 2: createDirectCompletion 包裹 withRetry**

`lib/ai/aiCaller.js` 第 193-204 行。旧：
```js
  async createDirectCompletion(apiCaller, config, messages, options = {}, fallback = false) {
    const model = fallback
      ? resolveFallbackWorkModel(config)
      : resolvePreferredWorkModel(config, options.model);
    return apiCaller.openai.chat.completions.create({
      model,
      messages: sanitizeMessages(messages),
      temperature: options.temperature ?? config.temperature ?? 0.7,
      max_tokens: options.max_tokens,
      stream: false,
    });
  }
```
新：
```js
  async createDirectCompletion(apiCaller, config, messages, options = {}, fallback = false) {
    const model = fallback
      ? resolveFallbackWorkModel(config)
      : resolvePreferredWorkModel(config, options.model);
    const retryCount = Number(config.retryCount) > 0 ? Number(config.retryCount) : 0;
    return withRetry(
      () => apiCaller.openai.chat.completions.create({
        model,
        messages: sanitizeMessages(messages),
        temperature: options.temperature ?? config.temperature ?? 0.7,
        max_tokens: options.max_tokens,
        stream: false,
      }),
      {
        retries: retryCount,
        onRetry: (info) => logger.warn(`[crystelf-ai] 请求重试 ${info.attempt}/${retryCount}，${info.delay}ms 后重试: ${info.error?.message || info.error}`),
      },
    );
  }
```

- [ ] **Step 3: createToolCompletion 包裹 withRetry**

`lib/ai/aiCaller.js` 第 206-218 行。旧：
```js
  async createToolCompletion(apiCaller, config, options = {}, fallback = false) {
    const model = fallback
      ? resolveFallbackWorkModel(config)
      : resolvePreferredWorkModel(config, options.model);
    return apiCaller.openai.chat.completions.create({
      model,
      messages: sanitizeMessages(options.messages || []),
      tools: options.tools,
      temperature: options.temperature ?? config.temperature ?? 0.7,
      max_tokens: options.max_tokens,
      stream: false,
    });
  }
```
新：
```js
  async createToolCompletion(apiCaller, config, options = {}, fallback = false) {
    const model = fallback
      ? resolveFallbackWorkModel(config)
      : resolvePreferredWorkModel(config, options.model);
    const retryCount = Number(config.retryCount) > 0 ? Number(config.retryCount) : 0;
    return withRetry(
      () => apiCaller.openai.chat.completions.create({
        model,
        messages: sanitizeMessages(options.messages || []),
        tools: options.tools,
        temperature: options.temperature ?? config.temperature ?? 0.7,
        max_tokens: options.max_tokens,
        stream: false,
      }),
      {
        retries: retryCount,
        onRetry: (info) => logger.warn(`[crystelf-ai] 请求重试 ${info.attempt}/${retryCount}，${info.delay}ms 后重试: ${info.error?.message || info.error}`),
      },
    );
  }
```

- [ ] **Step 4: init 调用 openaiChat.init 时传 retryCount**

`lib/ai/aiCaller.js` 第 86 行。旧：
```js
      this.openaiChat.init(this.config.apiKey, this.config.baseApi, this.config.timeout);
```
新：
```js
      this.openaiChat.init(this.config.apiKey, this.config.baseApi, this.config.timeout, this.config.retryCount);
```

- [ ] **Step 5: refreshRuntimeConfig 调用 openaiChat.init 时传 retryCount**

`lib/ai/aiCaller.js` 第 232 行。旧：
```js
    this.openaiChat.init(this.config.apiKey, this.config.baseApi, this.config.timeout);
```
新：
```js
    this.openaiChat.init(this.config.apiKey, this.config.baseApi, this.config.timeout, this.config.retryCount);
```

- [ ] **Step 6: getOpenaiInstanceForConfig 的 fallback 实例也传 retryCount**

`lib/ai/aiCaller.js` 第 138-140 行。旧：
```js
      const fallbackOpenaiChat = new OpenaiChat();
      fallbackOpenaiChat.init(config.apiKey, config.baseApi, config.timeout);
      return fallbackOpenaiChat;
```
新：
```js
      const fallbackOpenaiChat = new OpenaiChat();
      fallbackOpenaiChat.init(config.apiKey, config.baseApi, config.timeout, config.retryCount);
      return fallbackOpenaiChat;
```

- [ ] **Step 7: getConfigSignature 加 retryCount**

`lib/ai/aiCaller.js` 第 133 行（`getConfigSignature` 的 `return JSON.stringify({...})` 末尾、`imageFallbackApiKey` 之后）。在 `imageFallbackApiKey: ...` 那行后追加一行。旧：
```js
      imageFallbackApiKey: String(config.imageConfig?.fallbackApi?.apiKey || '').trim(),
    });
```
新：
```js
      imageFallbackApiKey: String(config.imageConfig?.fallbackApi?.apiKey || '').trim(),
      retryCount: Number(config.retryCount) > 0 ? Number(config.retryCount) : 0,
    });
```

- [ ] **Step 8: 语法校验**

Run: `node --check lib/ai/aiCaller.js`
Expected: 无输出（语法正确）。

- [ ] **Step 9: 提交**

```bash
git add lib/ai/aiCaller.js
git commit -m "feat(ai): wrap direct/tool completions with withRetry and propagate retryCount"
```

---

## Task 4: imageProcessor 接入重试（图像生成路径）

**Files:**
- Modify: `lib/ai/imageProcessor.js`

6 个底层方法（`generateImageByOpenAI`/`generateImageByChat`/`generateImageByJimeng`、`editImageByChat`/`editImageByOpenAI`/`editImageByJimeng`）各自的 `axios.post` 是真正会因网络/超时失败的地方。先让 `mergeImageConfig` 把全局 `retryCount` 继承进 mergedConfig，再逐个包裹。

- [ ] **Step 1: 顶部导入 withRetry**

`lib/ai/imageProcessor.js` 顶部 import 区（约第 1-10 行，在已有 import 语句之后）追加：
```js
import { withRetry } from './retry.js';
```
若不确定具体行，搜索文件第一处 `import` 后追加该行即可。

- [ ] **Step 2: mergeImageConfig 末尾继承全局 retryCount**

`lib/ai/imageProcessor.js` 第 730-733 行（`mergeImageConfig` 末尾 `return mergedConfig` 前）。旧：
```js
    mergedConfig.baseApi = normalizeOpenAiBaseApi(mergedConfig.baseApi || userConfig?.baseApi || '');
    mergedConfig.apiKey = String(mergedConfig.apiKey || userConfig?.apiKey || '').trim();

    return mergedConfig;
  }
```
新：
```js
    mergedConfig.baseApi = normalizeOpenAiBaseApi(mergedConfig.baseApi || userConfig?.baseApi || '');
    mergedConfig.apiKey = String(mergedConfig.apiKey || userConfig?.apiKey || '').trim();
    mergedConfig.retryCount = Number(userConfig?.retryCount) > 0 ? Number(userConfig.retryCount) : 0;

    return mergedConfig;
  }
```

注意：放在 jimeng 分支（第 724-728 行 `return mergedConfig`）之后，所以 jimeng 分支也需要继承。把第 724-728 行的 jimeng 早返回分支也补上。旧：
```js
    const imageMode = String(mergedConfig.imageMode || 'openai').trim();
    if (imageMode === 'jimeng') {
      mergedConfig.jimengApiUrl = String(mergedConfig.jimengApiUrl || '').trim();
      mergedConfig.apiKey = String(mergedConfig.apiKey || '').trim();
      return mergedConfig;
    }
```
新：
```js
    const imageMode = String(mergedConfig.imageMode || 'openai').trim();
    if (imageMode === 'jimeng') {
      mergedConfig.jimengApiUrl = String(mergedConfig.jimengApiUrl || '').trim();
      mergedConfig.apiKey = String(mergedConfig.apiKey || '').trim();
      mergedConfig.retryCount = Number(userConfig?.retryCount) > 0 ? Number(userConfig.retryCount) : 0;
      return mergedConfig;
    }
```

- [ ] **Step 3: 包裹 generateImageByOpenAI 的 axios.post**

`lib/ai/imageProcessor.js` 第 211-221 行（`generateImageByOpenAI` 内）。旧：
```js
      const response = await axios.post(
        buildOpenAiApiUrl(config.baseApi, '/v1/images/generations'),
        requestBody,
        {
          headers: {
            'Authorization': `Bearer ${config.apiKey}`,
            'Content-Type': 'application/json'
          },
          timeout: config.timeout || 60000
        }
      );
```
新：
```js
      const response = await withRetry(
        () => axios.post(
          buildOpenAiApiUrl(config.baseApi, '/v1/images/generations'),
          requestBody,
          {
            headers: {
              'Authorization': `Bearer ${config.apiKey}`,
              'Content-Type': 'application/json'
            },
            timeout: config.timeout || 60000
          }
        ),
        {
          retries: Number(config.retryCount) > 0 ? Number(config.retryCount) : 0,
          onRetry: (info) => logger.warn(`[crystelf-ai] 图像生成重试 ${info.attempt}/${config.retryCount}，${info.delay}ms 后重试: ${info.error?.message || info.error}`),
        },
      );
```

- [ ] **Step 4: 包裹 generateImageByChat 的 axios.post**

`lib/ai/imageProcessor.js` 第 282-292 行（`generateImageByChat` 内）。旧：
```js
      const response = await axios.post(
        buildOpenAiApiUrl(config.baseApi, '/v1/chat/completions'),
        requestBody,
        {
          headers: {
            'Authorization': `Bearer ${config.apiKey}`,
            'Content-Type': 'application/json'
          },
          timeout: config.timeout || 60000
        }
      );
```
新：
```js
      const response = await withRetry(
        () => axios.post(
          buildOpenAiApiUrl(config.baseApi, '/v1/chat/completions'),
          requestBody,
          {
            headers: {
              'Authorization': `Bearer ${config.apiKey}`,
              'Content-Type': 'application/json'
            },
            timeout: config.timeout || 60000
          }
        ),
        {
          retries: Number(config.retryCount) > 0 ? Number(config.retryCount) : 0,
          onRetry: (info) => logger.warn(`[crystelf-ai] 图像生成重试 ${info.attempt}/${config.retryCount}，${info.delay}ms 后重试: ${info.error?.message || info.error}`),
        },
      );
```

- [ ] **Step 5: 定位并包裹剩余 4 个方法的 axios.post**

剩余 4 个方法 `generateImageByJimeng`、`editImageByChat`、`editImageByOpenAI`、`editImageByJimeng` 各有 1 处 `axios.post`。它们的 `axios.post(url, body, config)` 形态一致，逐个用同样的 `withRetry(() => axios.post(...), { retries: Number(config.retryCount) > 0 ? Number(config.retryCount) : 0, onRetry: ... })` 包裹。

对每个方法：
1. 用 Grep 定位：`grep -n "axios.post" lib/ai/imageProcessor.js`，会列出所有 6 处的行号。
2. 对第 3、4 步已处理的 2 处（`generateImageByOpenAI`、`generateImageByChat`）跳过。
3. 对其余 4 处，把 `const response = await axios.post(URL, BODY, OPTS);` 改为：
```js
      const response = await withRetry(
        () => axios.post(URL, BODY, OPTS),
        {
          retries: Number(config.retryCount) > 0 ? Number(config.retryCount) : 0,
          onRetry: (info) => logger.warn(`[crystelf-ai] 图像生成重试 ${info.attempt}/${config.retryCount}，${info.delay}ms 后重试: ${info.error?.message || info.error}`),
        },
      );
```
（URL/BODY/OPTS 保持该处原有的实参不变，只把调用包进箭头函数。）

完成后用 `grep -c "withRetry" lib/ai/imageProcessor.js` 确认计数为 7（1 处 import + 6 处包裹）。

- [ ] **Step 6: 语法校验**

Run: `node --check lib/ai/imageProcessor.js`
Expected: 无输出（语法正确）。

- [ ] **Step 7: 提交**

```bash
git add lib/ai/imageProcessor.js
git commit -m "feat(ai): wrap image generation axios.post calls with withRetry"
```

---

## Task 5: 配置默认值 + Guoba schema 设置项 + 校验

**Files:**
- Modify: `config/ai.json`
- Modify: `guoba/configSchema.js`
- Modify: `guoba/configHandler.js`

网页控制台与 guoba 共用 `guoba/configSchema.js`，加一处即可被网页控制台自动渲染/校验/保存，前端零改动。

- [ ] **Step 1: config/ai.json 加默认值**

`config/ai.json` 第 66-67 行（`timeout` 之后）。旧：
```json
  "?timeout": "AI调用超时时间(毫秒)",
  "timeout": 60000,
```
新：
```json
  "?timeout": "AI调用超时时间(毫秒)",
  "timeout": 60000,
  "?retryCount": "AI接口调用失败时的重试次数，0表示不重试(默认)，仅对网络错误/超时/429/5xx生效，重试耗尽后仍会尝试备用API",
  "retryCount": 0,
```

- [ ] **Step 2: guoba/configSchema.js 加设置项**

`guoba/configSchema.js` 第 1634 行（`ai.timeout` 设置项的闭合 `},` 之后、`ai.emojiSuppression` 之前）插入新设置项。旧：
```js
  {
    field: 'ai.timeout',
    label: 'AI调用超时时间',
    component: 'InputNumber',
    bottomHelpMessage: 'AI 请求超时时间，单位毫秒',
    componentProps: {
      min: 1,
      max: 300000,
      step: 1000,
      placeholder: '请输入超时时间(毫秒)',
    },
  },
  {
    field: 'ai.emojiSuppression',
```
新：
```js
  {
    field: 'ai.timeout',
    label: 'AI调用超时时间',
    component: 'InputNumber',
    bottomHelpMessage: 'AI 请求超时时间，单位毫秒',
    componentProps: {
      min: 1,
      max: 300000,
      step: 1000,
      placeholder: '请输入超时时间(毫秒)',
    },
  },
  {
    field: 'ai.retryCount',
    label: 'AI调用重试次数',
    component: 'InputNumber',
    bottomHelpMessage: 'AI 接口失败时的重试次数，0 表示不重试；仅对网络错误/超时/429/5xx 重试，指数退避(500ms起,上限8s)；重试耗尽后仍会尝试备用API',
    componentProps: {
      min: 0,
      max: 10,
      step: 1,
      placeholder: '请输入重试次数，0表示不重试',
    },
  },
  {
    field: 'ai.emojiSuppression',
```

- [ ] **Step 3: guoba/configHandler.js 加范围校验**

`guoba/configHandler.js` 第 340-342 行（`maxIterations` 校验之后）。旧：
```js
      if (config.maxIterations !== undefined && Number(config.maxIterations) < -1) {
        errors.push('最大推理轮次不能小于 -1');
      }
```
新：
```js
      if (config.maxIterations !== undefined && Number(config.maxIterations) < -1) {
        errors.push('最大推理轮次不能小于 -1');
      }
      pushRangeError(errors, config.retryCount, 0, 10, 'AI调用重试次数必须在 0-10 之间');
```

- [ ] **Step 4: 语法校验**

Run: `node --check guoba/configSchema.js && node --check guoba/configHandler.js`
Expected: 无输出（语法正确）。

- [ ] **Step 5: 校验 ai.json 仍是合法 JSON**

Run: `node -e "JSON.parse(require('fs').readFileSync('config/ai.json','utf8')); console.log('ai.json OK')"`
Expected: 输出 `ai.json OK`

- [ ] **Step 6: 提交**

```bash
git add config/ai.json guoba/configSchema.js guoba/configHandler.js
git commit -m "feat(ai): expose ai.retryCount config, schema field and validation"
```

---

## Task 6: 集成验证 + 网页控制台设置项确认

**Files:**
- 无新文件，只做端到端验证

确认所有改动协同工作：重试工具、四个调用点、配置、schema、校验、热生效签名。

- [ ] **Step 1: 全量语法校验所有改动文件**

Run: `node --check lib/ai/retry.js && node --check modules/openai/openaiChat.js && node --check lib/ai/aiCaller.js && node --check lib/ai/imageProcessor.js && node --check guoba/configSchema.js && node --check guoba/configHandler.js`
Expected: 无输出（全部语法正确）

- [ ] **Step 2: 重试单元测试仍通过**

Run: `node --test lib/ai/retry.test.mjs`
Expected: 全部 PASS

- [ ] **Step 3: 确认 imageProcessor withRetry 计数**

Run: `grep -c "withRetry" lib/ai/imageProcessor.js`
Expected: `7`（1 import + 6 包裹）

Run: `grep -c "withRetry" modules/openai/openaiChat.js`
Expected: `2`（1 import + 1 包裹）

Run: `grep -c "withRetry" lib/ai/aiCaller.js`
Expected: `3`（1 import + 2 包裹）

- [ ] **Step 4: 确认 getConfigSignature 含 retryCount**

Run: `grep -n "retryCount" lib/ai/aiCaller.js`
Expected: 多处匹配，包括 `getConfigSignature` 内的 `retryCount:` 行、`init`/`refreshRuntimeConfig`/`getOpenaiInstanceForConfig` 的传参行、两个 completion 方法的 `Number(config.retryCount)` 行。

- [ ] **Step 5: 确认 schema 设置项存在且字段正确**

Run: `grep -n "ai.retryCount" guoba/configSchema.js`
Expected: 1 处匹配，在 `ai.timeout` 之后、`ai.emojiSuppression` 之前。

- [ ] **Step 6: 确认网页控制台能渲染该设置项（不依赖运行时）**

网页控制台通过 `buildPluginSettingsPayload` 遍历 guobaSchema 渲染，无需改动前端。逻辑验证：确认 `ai.retryCount` 的 `field` 前缀为 `ai`，会被 `getSettingsCategory` 归入 `ai` 分类；`component: 'InputNumber'` 会被前端 `renderField` 的 InputNumber 分支渲染。这些是现有通用逻辑，新设置项自动继承，无需额外代码。

Run: `grep -rn "InputNumber" lib/webConsole/public/plugin-settings-render.js`
Expected: 有匹配，证明前端已支持 InputNumber 渲染。

- [ ] **Step 7: 运行项目自带检查脚本（若可用）**

Run: `npm run check 2>&1 | head -30`
Expected: 检查通过或仅与本功能无关的既有告警。若脚本报本功能相关文件有编码/结构问题，按提示修正。

- [ ] **Step 8: 最终提交（若有未提交的微调）**

```bash
git status
# 若有未提交改动：
git add -A && git commit -m "chore(ai): retry mechanism integration verification"
```

---

## Self-Review（写完后自查，已修正）

1. **Spec 覆盖**：spec 第 5 节四处接入点 → Task 2/3/4 覆盖；spec 第 6 节配置/schema/校验/热生效 → Task 5 + Task 3 Step 7 覆盖；spec 第 4 节 retry 模块 → Task 1 覆盖；spec 第 8 节「不改动」项（fallback、poke、SDK maxRetries）计划未触碰，符合。✓
2. **占位符**：无 TBD/TODO，Task 4 Step 5 对剩余 4 处给出了明确包裹模板与定位方法（grep + 套模板），非占位符。✓
3. **类型/命名一致**：`withRetry` 签名在 Task 1 定义，Task 2/3/4 调用参数（retries/onRetry）一致；`retryCount` 字段名在 config/schema/configHandler/getConfigSignature/各调用点统一；`isRetryableError` 在 Task 1 定义并被 `withRetry` 内部使用。✓
4. **图像 jimeng 分支**：spec 自检已发现 mergeImageConfig 的 jimeng 早返回分支需单独继承 retryCount，Task 4 Step 2 已补上。✓
