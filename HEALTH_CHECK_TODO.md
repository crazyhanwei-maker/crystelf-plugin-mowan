# 健康检查说明

本文档面向插件使用者，说明如何确认插件和控制台是否处于正常状态。

## 推荐检查顺序

1. 启动 Bot，确认插件成功加载。
2. 打开魔丸控制台，查看首页“控制台健康总览”。
3. 进入“依赖检查”，确认运行依赖没有缺失。
4. 进入“API 设置”，按需测试主对话、图像生成、图片监控和表情包接口。
5. 在测试群发送 `#灵晶状态` 和 `#灵晶帮助`。

## 本地检查命令

在插件目录执行：

```bash
npm run check
```

这个命令会检查：

- 文本编码是否正常。
- 控制台页面引用是否完整。
- 主要模块结构是否完整。
- 控制台前端 JavaScript 是否存在语法错误。

## 控制台自检

控制台首页会展示：

- 控制台是否正在运行。
- 启动自检结果。
- 依赖是否完整。
- 当前版本状态。
- 页面加载缓存状态。
- 前端异常捕获结果。
- 访问安全提示。
- 审计日志概览。

如果出现红色异常，建议优先点进对应卡片查看详情。

## 常见处理方式

### 依赖缺失

进入“依赖检查”页面，使用一键修复功能；如果仍失败，再查看失败原因和智能建议。

### 控制台打不开

确认 Bot 正在运行，并检查：

- `webConsole` 是否开启。
- 端口是否被占用。
- 登录口令是否正确。
- 防火墙是否允许访问。

### 页面按钮无响应或页面白屏

首页“前端异常”卡片会记录当前浏览器捕获到的脚本、接口和资源加载异常。可以打开详情面板，复制诊断信息后再排查。

### 公网访问风险

如果控制台监听外部网卡，请确认：

- 已设置强登录口令。
- 只允许可信来源访问。
- 不需要修改配置时可以开启只读模式。

## 维护建议

- 更新插件后重启 Bot。
- 修改控制台访问、安全或依赖相关配置后，重新打开控制台确认状态。
- 不建议直接修改运行数据文件；优先使用控制台或锅巴配置。
- 重要配置变更前，先在首页使用“一键备份配置”导出备份。
