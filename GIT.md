# Git / Gitee 安装与更新

这份文档写给插件使用者。你不需要理解 Git 的全部细节，只要照着这里安装、更新和排错，就能让魔丸插件保持在可维护的状态。

如果你只是第一次安装，直接看“推荐安装方式”。如果你已经装过插件，平时只需要看“更新插件”和“群内依赖修复”。

## 1. 推荐安装方式

先进入 Yunzai 根目录，然后克隆插件：

```bash
git clone --depth=1 https://gitee.com/nuesurwan/crystelf-plugin.git ./plugins/crystelf-plugin
```

然后在 Yunzai 根目录安装依赖。大多数 Yunzai 环境使用 `pnpm`：

```bash
pnpm install
```

如果你的 Bot 环境一直使用 `npm`，也可以执行：

```bash
npm install
```

完成后重启 Bot。插件加载成功后，可以发送：

```text
#灵晶状态
```

如果能收到状态图，说明插件已经正常工作。

## 2. 更新插件

服务器上更新时，先进入插件目录：

```bash
cd ./plugins/crystelf-plugin
```

然后拉取最新版本：

```bash
git pull
```

你也可以直接在 QQ 里让主人发送：

```text
#更新灵晶
```

普通更新不会覆盖你本地没提交的改动。如果 Git 提示有本地修改无法合并，先不要急着乱试命令，优先备份插件目录。

如果你确认插件目录里的本地改动都可以丢弃，可以由主人发送：

```text
#强制更新灵晶
```

Bot 会先显示当前版本、远端版本和风险提示。确认没问题后，在 3 分钟内发送：

```text
确认强制更新灵晶
```

不想继续就发送：

```text
取消强制更新灵晶
```

强制更新会覆盖插件目录中的未提交改动，并清理未跟踪文件。这个功能适合“插件代码被改乱了，想恢复到远端版本”的场景；如果你改过配置或有重要数据，先备份再用。

更新控制台、群管理、图片监控、模拟调试、AI 生图、插件管理或依赖修复功能后，建议重启 Bot，让新代码完整生效。

## 3. 查看当前版本

查看当前 Git 提交：

```bash
git log -1 --oneline
```

查看插件版本号：

```bash
node -p "require('./package.json').version"
```

当前文档对应版本：

```text
2.3.1
```

## 4. ZIP 安装怎么处理

如果你之前是下载 ZIP 后手动放进 `plugins/crystelf-plugin`，目录里通常没有 `.git`。这种安装方式不能直接 `git pull`，也不能使用群内更新。

推荐处理方式：

1. 先备份插件目录。
2. 如果只想保留配置，重点备份 `config/` 和 `data/crystelf/`。
3. 删除旧的 `plugins/crystelf-plugin`。
4. 用本文第一节的 `git clone` 方式重新安装。
5. 把配置和数据恢复回去。
6. 重启 Bot。

常见需要保留的配置：

```text
config/config.json
config/ai.json
config/imageMonitor.json
config/groupTitle.json
config/skills.json
config/skills/
```

运行时数据通常在：

```text
data/crystelf/
```

如果你不确定某个文件能不能删，最稳妥的做法是先备份整个插件目录。

## 5. 拉取失败怎么办

### 网络连接失败

先稍后重试：

```bash
git pull
```

如果服务器访问 Gitee 不稳定，可以先在本地更新插件，再同步到服务器。

### 提示不是 Git 仓库

这通常说明当前目录不是通过 `git clone` 安装的，或者你没有进入插件目录。

查看当前目录：

```bash
pwd
```

确认目录里有没有 `.git`：

```bash
ls -a
```

Windows PowerShell 可以用：

```powershell
Get-ChildItem -Force
```

### 提示本地修改无法合并

这说明插件目录里有本地改动。普通使用者建议按这个顺序处理：

1. 备份整个插件目录。
2. 如果只改过配置，先保留 `config/` 和 `data/crystelf/`。
3. 重新 clone 一份干净插件。
4. 再恢复配置和数据。

不要随便执行会清空改动的命令，尤其不要在没备份时强行覆盖。强制更新虽然方便，但它的设计目标就是“丢弃本地修改并回到远端版本”。

## 6. 群内依赖修复

如果插件或其他 Bot 插件缺少依赖，主人可以发送：

```text
#灵晶修复依赖
```

这个命令会检查灵晶插件和其他插件的缺失依赖，并为可修复的依赖创建安装任务。查看进度：

```text
#灵晶修复依赖状态
```

群主、管理员或主人也可以在群聊使用更短的命令：

```text
#修复依赖
```

Bot 会先列出准备修复的依赖，并等待确认。确认执行：

```text
确认修复依赖
```

取消执行：

```text
取消修复依赖
```

通过 `#修复依赖` 确认后的任务完成后，如果流程没有阻塞，Bot 会自动重启。

如果依赖安装失败，Bot 会先显示原始失败项。若已经配置可用的 LLM API，还会追加“AI 中文诊断”，把 npm、pnpm、yarn 的报错解释成中文，并给出更容易照做的修复建议。AI 诊断失败不会影响原来的修复结果显示。

## 7. 推荐更新顺序

手动维护服务器时，推荐这样做：

1. 停止 Bot。
2. 进入 `plugins/crystelf-plugin`。
3. 执行 `git pull`。
4. 如果依赖清单有变化，在 Yunzai 根目录执行 `pnpm install` 或 `npm install`。
5. 启动 Bot。
6. 打开控制台确认页面能正常加载。
7. 在测试群发送 `#灵晶状态` 和 `#灵晶帮助`。

如果你是通过群内 `#更新灵晶` 更新，更新完成后也建议重启一次 Bot。

## 8. 控制台与安全提醒

控制台默认地址：

```text
http://127.0.0.1:27891/
```

首次使用请查看启动日志中的控制台登录口令。后续可以在配置里改成自己的强口令。

几个建议：

- 不建议把可写控制台直接暴露到公网。
- `启动控制台.cmd` 不会写死口令，也不会自动开放防火墙。
- 如果需要局域网访问，请自己确认监听地址、登录口令和防火墙规则。
- 插件管理里的“安装插件”默认只克隆仓库，不会默认执行依赖安装脚本。
- 如果手动选择安装依赖，会按 `packageManager` 和锁文件自动选择 `pnpm`、`npm` 或 `yarn`，并执行 `install --ignore-scripts`。
- 群内依赖修复会安装缺失依赖，建议只允许可信管理员使用。

## 9. 常用命令

服务器命令：

```bash
git clone --depth=1 https://gitee.com/nuesurwan/crystelf-plugin.git ./plugins/crystelf-plugin
git pull
git log -1 --oneline
```

群内命令：

```text
#更新灵晶
#强制更新灵晶
确认强制更新灵晶
取消强制更新灵晶
#灵晶修复依赖
#灵晶修复依赖状态
#修复依赖
#修复依赖状态
确认修复依赖
取消修复依赖
#灵晶状态
#灵晶帮助
#灵晶健康报告
#灵晶备份列表
#灵晶立即备份
#灵晶任务
#灵晶水位
#灵晶水位阈值 磁盘 85
#群风控日报
#AI周报
#灵晶重启
确认重启灵晶
取消重启灵晶
```

## 10. 更新记录

### 2.3.4

- 新增每日运维晨报：每天定时（`morningReportHour` 可调，默认 8 点）聚合看门狗事件、昨日 AI 用量、备份状态与资源水位推送给主人；`#灵晶晨报` 预览、`#灵晶发晨报` 立即发送，任务中心新增晨报卡片支持手动触发。**默认关闭**，需要的话在配置里把 `morningReportEnabled` 设为 true。
- 晨报含本周 AI 用量环比（本周 vs 上周请求次数对比）；看门狗新增近 24 小时掉线/恢复事件时间线，晨报与状态接口均可读。
- 控制台响应 gzip 压缩：API 与静态资源按 `Accept-Encoding` 协商压缩（≥1KB 才压），实测用量日志接口传输量下降 93%，弱网/流量环境加载显著提速。
- 静态资源长缓存：css/js 引用 URL 自带版本号（文件变更即换 URL），浏览器缓存 24 小时，二次访问几乎零请求；图片/字体维持 ETag 协商，HTML 维持不缓存以保登录态。
- 控制台总览顶部新增运维监控横幅：QQ 适配器在线状态/掉线时长、邮件告警配置、近 24 小时连接事件一屏速览。

### 2.3.3

- 新增 QQ 运维命令（主人）：`#灵晶健康报告`（完整体检报告文本，可转发给维护者）、`#灵晶备份列表` / `#灵晶立即备份`（配置备份查询与手动执行）、`#灵晶任务`（四个定时任务状态与下次运行时间）、`#灵晶重启`（二次确认后重启 Bot，私聊或任意群均可确认）。
- 新增 `#群风控日报`（管理员）：近 7 天风控统计；`#AI周报`（所有人）：近 7 天 AI 用量按天统计。
- 新增 QQ 适配器掉线看门狗：每分钟检查适配器连接，掉线（2 分钟确认）与恢复分别通知主人；掉线通知通过持久化队列无限期保留，QQ 恢复瞬间送达，完整还原掉线时间线。
- 新增邮件告警旁路：配置 SMTP 后，掉线等关键告警同时发邮件（零依赖实现，支持 465 SSL 与 587 STARTTLS）；未配置时自动禁用。
- 新增资源水位看门狗：每 10 分钟检查磁盘 / 系统内存 / Redis 内存，超阈值 QQ+邮件告警，持续超限每 6 小时重提醒，恢复后通知。阈值可配置（`resourceDiskPercent` 等，50-100）。
- 新增 `#灵晶水位` 查看当前水位与阈值，`#灵晶水位阈值 磁盘 85` 直接调整告警线。
- 控制台总览新增「适配器看门狗」状态卡片与 `/api/bot-watchdog` 接口。
- 修复登录通知死循环：masterQQ 含 Yunzai 内置 `stdin` 伪账号时不再无限重发；送达判定改为至少一位主人收到即出队。
- 修复 `#合成语音` 在白名单群静默无反应：whiteGroup 字符串与 group_id 数字类型不匹配导致 includes 漏判（同模式修复 RSS 订阅群列表与聊天库查询）。
- 修复任务中心「立即拉取推送」按钮无效（action 名占位符残留）。
- SMTP 收发件地址连接前校验，拒绝 CRLF 头部注入。

### 2.3.2

- 新增 `#查看进程` 群聊命令：以图片发送系统进程列表（内存降序、支持按进程名/PID 过滤），复用网页控制台性能监测的进程数据。
- 新增 `#杀死PID 1234` 群聊命令（主人专用）与进程查看器「结束」按钮：Windows 走 taskkill /F /T、Linux 走 kill -9；内置保护名单，拒绝结束 bot 自身、sshd、lsass、systemd 等关键进程。
- 新增 `#灵晶查看更新日志` 命令：解析 GIT.md 更新记录章节，渲染成图片发送。
- 修复群聊抢命令问题：`in()` 与 `handleMessage` 入口统一放行命令前缀消息，昵称规则误命中、伪人、接话、冷却/延迟重放路径均不再接管 `#` / `/` 等前缀命令。
- 修复扫码登录后被抢问题：私聊 AI 不再接管米哈游登录插件下发的 token 绑定消息（stoken/ltoken/cookie_token/v2_ 开头），不再产生误警告。
- 新增私聊安全「不明确判定处置」选项：LLM 复审判定为不明确(warn)时可选放行(allow，默认)或警告(block)，明确违规(block)不受影响。
- AI 用量新增对话记录：记录用户发送和 AI 回复的完整内容、耗时和 token，可通过 `usageControl.chatContent` 开关控制；用量中心详情弹窗展示对话全文。
- 用量中心默认每页 10 条，「AI 用量日志」整块默认折叠。
- 新增机器人身份与人设预设保存/恢复功能，配置变更与历史回滚打通。
- 新增聊天功能开关命令：`#灵晶 开启/关闭 戳一戳|表情回复|早晚安|帮助`、`#灵晶 功能状态`。
- Bot 日志查看器：错误渲染红色、警告渲染黄色，并对 `[时间][MARK][详情][动作]` 行做结构化着色，保留原始 ANSI 颜色映射。

### 2.3.1

- 更新检查支持多源回退：第一源 Gitee 不可用时，自动切换到 GitHub 备用仓库 `crystelf-plugin-mowan`，群内更新与控制台版本检查同步生效。
- 同步更新插件 User-Agent、状态图和官网版本文案到 2.3.1。

### 2.2.0

- 新增受控 LLM 命令桥接，可将自然语言请求定向转换为人工授权的其他 Yunzai 插件命令。
- 命令中心新增运行时命令发现、搜索、逐条授权以及直接执行或二次确认设置。
- 桥接功能默认关闭且授权列表为空，高权限和高风险命令强制由同一用户二次确认。
- 新增 Agent 工作台，内置固定版本 OpenCode 并复用聊天默认 API；支持只读分析、补丁建议，以及默认关闭的受控文件修改模式。
- Agent 实际修改要求单独开启、授权工作目录、确认任务并保持 Git 工作区干净；终端命令、外部目录、联网工具和自动批准保持禁用。
- 同步更新插件 User-Agent、状态图和官网版本文案到 2.2.0。

### 2.1.0

- 新增群总结诊断页面，可集中查看发送、失败、跳过、重复触发、运行锁和消息统计。
- 重新整理控制台功能分类，并统一优化侧栏、顶部栏、面板、表单和移动端布局。
- 同步更新插件 User-Agent、状态图和官网版本文案到 2.1.0。

### 2.0.3

- 控制台安全增强：配置备份导出接口不再返回明文密钥，敏感字段统一遮蔽为 `******`；恢复备份时会自动从当前运行配置回填真实值，备份往返不会丢失密钥。
- 控制台安全增强：启动日志中的登录口令改为脱敏输出，不再以明文记录到应用日志。
- 同步更新插件 User-Agent 与状态图版本文案到 2.0.3。

### 2.0.2

- 完善控制台与 LLM 请求配置，优化依赖修复流程。
- 群内新增合成语音命令与语音模型切换，并修复切换命令。
- 提高合成语音与语音模型命令的优先级。
- 新增控制台一次性登录命令，完善语音场景说明。
- 同步更新插件 User-Agent 与状态图版本文案到 2.0.2。

### 2.0.1

- 帮助 DIY 控制台新增「生成图片」按钮，支持把帮助文本直接渲染为 PNG 作为 `#灵晶帮助` 发送图。
- 复用 Markdown 渲染器，与功能开关帮助图同一套渲染管线，生成的图片自动落到 `/uploads/help-diy/` 并替换旧图。
- 同步更新插件 User-Agent 与状态图版本文案到 2.0.1。

### 2.0.0

- 插件升级为 AI、群管理、图片监控、模拟调试和本地控制台的一体化方案。
- 新增 Bot 插件管理与插件目录，支持扫描、安装、依赖检查和删除到回收站。
- 群管理加入默认设置、加群申请列表、自动审核、AI 欢迎、群总结、头衔申请、URL 安检和刷屏检测。
- 模拟调试支持类 QQ 群界面、图片、语音、截图、戳一戳、回复、CQ 码、meme 和场景回归。
- 帮助系统和状态图重做，支持图片帮助、分类帮助、AI 用量、生图用量和最近告警。
- 控制台安全增强：登录鉴权、CSRF、Same-Origin、只读模式、敏感配置遮蔽和审计记录。
