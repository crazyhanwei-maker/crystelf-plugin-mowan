# Git / Gitee 安装与更新

这份文档写给插件使用者。你不需要理解 Git 的全部细节，只要照着这里安装、更新和排错，就能让魔丸插件保持在可维护的状态。

如果你只是第一次安装，直接看“推荐安装方式”。如果你已经装过插件，平时只需要看“更新插件”和“群内依赖修复”。

## 1. 推荐安装方式

先进入 Yunzai 根目录，然后克隆插件：

```bash
git clone --depth=1 https://gitee.com/nuesurwan/crystelf-plugin.git ./plugins/crystelf-plugin
```

然后在 Yunzai 根目录安装依赖。大多数 Yunzai 环境使用 `pnpm`：

```bash
pnpm install
```

如果你的 Bot 环境一直使用 `npm`，也可以执行：

```bash
npm install
```

完成后重启 Bot。插件加载成功后，可以发送：

```text
#灵晶状态
```

如果能收到状态图，说明插件已经正常工作。

## 2. 更新插件

服务器上更新时，先进入插件目录：

```bash
cd ./plugins/crystelf-plugin
```

然后拉取最新版本：

```bash
git pull
```

你也可以直接在 QQ 里让主人发送：

```text
#更新灵晶
```

普通更新不会覆盖你本地没提交的改动。如果 Git 提示有本地修改无法合并，先不要急着乱试命令，优先备份插件目录。

如果你确认插件目录里的本地改动都可以丢弃，可以由主人发送：

```text
#强制更新灵晶
```

Bot 会先显示当前版本、远端版本和风险提示。确认没问题后，在 3 分钟内发送：

```text
确认强制更新灵晶
```

不想继续就发送：

```text
取消强制更新灵晶
```

强制更新会覆盖插件目录中的未提交改动，并清理未跟踪文件。这个功能适合“插件代码被改乱了，想恢复到远端版本”的场景；如果你改过配置或有重要数据，先备份再用。

更新控制台、群管理、图片监控、模拟调试、AI 生图、插件管理或依赖修复功能后，建议重启 Bot，让新代码完整生效。

## 3. 查看当前版本

查看当前 Git 提交：

```bash
git log -1 --oneline
```

查看插件版本号：

```bash
node -p "require('./package.json').version"
```

当前文档对应版本：

```text
2.0.2
```

## 4. ZIP 安装怎么处理

如果你之前是下载 ZIP 后手动放进 `plugins/crystelf-plugin`，目录里通常没有 `.git`。这种安装方式不能直接 `git pull`，也不能使用群内更新。

推荐处理方式：

1. 先备份插件目录。
2. 如果只想保留配置，重点备份 `config/` 和 `data/crystelf/`。
3. 删除旧的 `plugins/crystelf-plugin`。
4. 用本文第一节的 `git clone` 方式重新安装。
5. 把配置和数据恢复回去。
6. 重启 Bot。

常见需要保留的配置：

```text
config/config.json
config/ai.json
config/imageMonitor.json
config/groupTitle.json
config/skills.json
config/skills/
```

运行时数据通常在：

```text
data/crystelf/
```

如果你不确定某个文件能不能删，最稳妥的做法是先备份整个插件目录。

## 5. 拉取失败怎么办

### 网络连接失败

先稍后重试：

```bash
git pull
```

如果服务器访问 Gitee 不稳定，可以先在本地更新插件，再同步到服务器。

### 提示不是 Git 仓库

这通常说明当前目录不是通过 `git clone` 安装的，或者你没有进入插件目录。

查看当前目录：

```bash
pwd
```

确认目录里有没有 `.git`：

```bash
ls -a
```

Windows PowerShell 可以用：

```powershell
Get-ChildItem -Force
```

### 提示本地修改无法合并

这说明插件目录里有本地改动。普通使用者建议按这个顺序处理：

1. 备份整个插件目录。
2. 如果只改过配置，先保留 `config/` 和 `data/crystelf/`。
3. 重新 clone 一份干净插件。
4. 再恢复配置和数据。

不要随便执行会清空改动的命令，尤其不要在没备份时强行覆盖。强制更新虽然方便，但它的设计目标就是“丢弃本地修改并回到远端版本”。

## 6. 群内依赖修复

如果插件或其他 Bot 插件缺少依赖，主人可以发送：

```text
#灵晶修复依赖
```

这个命令会检查灵晶插件和其他插件的缺失依赖，并为可修复的依赖创建安装任务。查看进度：

```text
#灵晶修复依赖状态
```

群主、管理员或主人也可以在群聊使用更短的命令：

```text
#修复依赖
```

Bot 会先列出准备修复的依赖，并等待确认。确认执行：

```text
确认修复依赖
```

取消执行：

```text
取消修复依赖
```

通过 `#修复依赖` 确认后的任务完成后，如果流程没有阻塞，Bot 会自动重启。

如果依赖安装失败，Bot 会先显示原始失败项。若已经配置可用的 LLM API，还会追加“AI 中文诊断”，把 npm、pnpm、yarn 的报错解释成中文，并给出更容易照做的修复建议。AI 诊断失败不会影响原来的修复结果显示。

## 7. 推荐更新顺序

手动维护服务器时，推荐这样做：

1. 停止 Bot。
2. 进入 `plugins/crystelf-plugin`。
3. 执行 `git pull`。
4. 如果依赖清单有变化，在 Yunzai 根目录执行 `pnpm install` 或 `npm install`。
5. 启动 Bot。
6. 打开控制台确认页面能正常加载。
7. 在测试群发送 `#灵晶状态` 和 `#灵晶帮助`。

如果你是通过群内 `#更新灵晶` 更新，更新完成后也建议重启一次 Bot。

## 8. 控制台与安全提醒

控制台默认地址：

```text
http://127.0.0.1:27891/
```

首次使用请查看启动日志中的控制台登录口令。后续可以在配置里改成自己的强口令。

几个建议：

- 不建议把可写控制台直接暴露到公网。
- `启动控制台.cmd` 不会写死口令，也不会自动开放防火墙。
- 如果需要局域网访问，请自己确认监听地址、登录口令和防火墙规则。
- 插件管理里的“安装插件”默认只克隆仓库，不会默认执行依赖安装脚本。
- 如果手动选择安装依赖，会按 `packageManager` 和锁文件自动选择 `pnpm`、`npm` 或 `yarn`，并执行 `install --ignore-scripts`。
- 群内依赖修复会安装缺失依赖，建议只允许可信管理员使用。

## 9. 常用命令

服务器命令：

```bash
git clone --depth=1 https://gitee.com/nuesurwan/crystelf-plugin.git ./plugins/crystelf-plugin
git pull
git log -1 --oneline
```

群内命令：

```text
#更新灵晶
#强制更新灵晶
确认强制更新灵晶
取消强制更新灵晶
#灵晶修复依赖
#灵晶修复依赖状态
#修复依赖
#修复依赖状态
确认修复依赖
取消修复依赖
#灵晶状态
#灵晶帮助
```
