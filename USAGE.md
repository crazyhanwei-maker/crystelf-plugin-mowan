# crystelf-plugin 使用手册

本文档面向插件使用者，按“先能跑起来，再逐步开启功能”的顺序整理。

## 1. 首次启动

1. 确认插件位于 Yunzai 的 `plugins/crystelf-plugin`。
2. 在 Yunzai 根目录安装依赖：`pnpm install` 或 `npm install`。
3. 启动 Bot。
4. 查看启动日志里的“控制台登录口令”；未配置时会自动生成并写入运行配置。
5. 打开控制台：`http://127.0.0.1:27891/`，局域网/公网访问请使用服务器 IP 和同一端口。
6. 进入“API 设置”，配置主对话接口、图像生成接口、图片监控接口。
7. 进入“插件设置”，按需开启功能。
8. 进入“群管理”，选择要启用的群。群管理相关功能默认关闭。

AI 主接口的 `baseApi` 填 OpenAI 兼容基地址，例如：

```text
https://example.com/v1
```

不要填到 `/chat/completions`。

## 2. 功能开关

默认配置来自 `config/config.json`。危险或会接管群行为的功能默认关闭。

| 功能 | 默认 | 说明 |
| --- | --- | --- |
| `webConsole` | 开启 | 本地控制台 |
| `ai` | 开启 | AI 对话 |
| `help` | 开启 | `#灵晶帮助` |
| `60s` | 开启 | 60 秒早报 |
| `rss` | 开启 | RSS 订阅 |
| `music` | 开启 | 点歌 |
| `poke` | 开启 | 戳一戳互动 |
| `zwa` | 开启 | 早晚安 |
| `faceReply` | 开启 | 表情回应 |
| `welcome` | 关闭 | 入群欢迎 |
| `auth` | 关闭 | 入群验证 |
| `imageMonitor` | 关闭 | 图片监控 |
| `groupManagement` | 关闭 | 群管理 |
| `groupTitle` | 关闭 | 群头衔申请 |
| `autoUpdate` | 关闭 | 自动更新 |

## 3. 魔丸控制台

默认入口：

```text
http://127.0.0.1:27891/
```

控制台默认监听 `0.0.0.0`，会绑定所有网卡。未配置 `webConsoleToken` 时，启动过程会自动生成随机口令、写入运行配置，并在启动日志里显示“控制台登录口令”。如果暴露到公网，请限制访问来源并改成自己的强口令。

控制台页面：

| 页面 | 路径 | 说明 |
| --- | --- | --- |
| 总览 | `/index.html` | 健康巡检、状态概览、日志排查 |
| 插件设置 | `/plugin-settings.html` | 功能开关、AI、Skills、控制台主题 |
| API 设置 | `/api-settings.html` | 主接口、生图、图片监控、搜索接口测试 |
| Bot 插件管理 | `/bot-plugins.html` | 已装插件、插件目录、安装、删除 |
| 群管理 | `/group-management.html` | 群选择、默认设置、欢迎、风控、总结、头衔 |
| 图片监控 | `/image-monitor-center.html` | 审核记录、表情包入库、本地图片预览、清理 |
| 模拟调试 | `/qq-simulator.html` | 类 QQ 群测试环境 |
| 对话测试 | `/sandbox-chat.html` | 单独测试 AI 对话规则、工具、知识库 |
| 文件编辑 | `/file-browser.html` | 控制台内查看和编辑项目文件 |
| 依赖检查 | `/dependency-check.html` | 检查依赖缺失、版本不一致 |
| AI 用量 | `/usage-center.html` | 模型用量、场景、错误率统计 |
| 帮助 DIY | `/help-diy.html` | 自定义文字帮助和图片帮助 |

### 控制台安全

- 控制台使用 Cookie 会话、SameSite、CSRF 校验。
- HTML 页面未登录会被服务端跳转到登录页。
- 非本机监听前必须设置登录口令。
- 不建议把可写控制台直接暴露到公网。
- `启动控制台.cmd` 不再打印固定口令，也不自动开放防火墙。

## 4. AI 对话

常用能力：

- 多轮上下文。
- 用户画像和好感度。
- RAG 知识库。
- 图片理解。
- 工具调用。
- 语音回复。
- 表情包回复。
- 最近会话测试和工具调用排查。

常用命令：

| 命令 | 说明 |
| --- | --- |
| `重置对话` / `重置会话` | 清空当前会话上下文 |
| `查看好感度` | 查看用户好感 |
| `好感排行` | 查看群内好感排行 |
| `查看用户画像` | 查看画像摘要 |
| `查看会话状态` | 查看当前会话状态 |
| `查看知识命中` | 查看 RAG 命中 |
| `查看工具调用` | 查看最近工具调用 |

AI 可以在回复中使用表情包标记：

```text
[meme:芙宁娜:happy]
[meme:芙宁娜:default]
```

支持的情绪建议使用：`happy`、`sad`、`angry`、`confused`、`shy`、`surprised`、`default`。

## 5. AI 生图

生图使用自然语言触发，例如：

```text
花花生成一张美丽的花海图片
生成一张赛博风格的城市夜景
帮我画一张芙宁娜开心表情包
```

插件故意不抢：

```text
#绘图
#画图
```

这两个命令保留给其他绘图插件。

改图和多图融合使用灵晶自己的明确指令：

```text
#灵晶改图 把背景改成樱花海，保持人物不变
#灵晶融合 把两张图片融合成自然的合影
```

`#灵晶改图` 需要在同一条消息附带图片，或者回复需要修改的图片。`#灵晶融合` 至少需要两张参考图片，单次最多支持 14 张。群聊和私聊都可以使用。

图像配置在 `config/ai.json` 的 `imageConfig`，也可以在控制台“API 设置”修改。

| 配置 | 说明 |
| --- | --- |
| `enabled` | 是否启用生图 |
| `imageMode` | `openai`、`ark-agent-plan`、`sd-webui`、`chat`、`jimeng` |
| `baseApi` | 图像接口基地址 |
| `apiKey` | 图像接口密钥 |
| `model` | 例如 `gpt-image-2` 或 `doubao-seedream-5.0-lite` |
| `size` | OpenAI 可用 `1024x1024`；Agent Plan 可用 `2K`、`3K`、`4K` |
| `quality` | `gpt-image-2` 推荐 `low`、`medium`、`high` |
| `timeout` | 慢接口可设置 `120000` 或 `360000` |
| `responseFormat` | `url` 或 `b64_json`；`gpt-image-2` 通常推荐 `b64_json` |
| `outputFormat` | Agent Plan 图片格式：`png` 或 `jpeg` |
| `watermark` | Agent Plan 是否添加水印 |
| `webSearch` | Agent Plan 联网搜索开关，默认关闭 |

`openai` 模式会调用 `/v1/images/generations`。如果 `baseApi` 填了 `https://example.com/v1`，插件会自动处理最终路径。

### 火山方舟 Agent Plan

在控制台“API 设置 -> 图像生成 API”选择“火山 Agent Plan”后，页面只显示这一模式需要的字段。推荐配置：

| 配置 | 推荐值 |
| --- | --- |
| `imageMode` | `ark-agent-plan` |
| `baseApi` | `https://ark.cn-beijing.volces.com/api/plan/v3` |
| `model` | `doubao-seedream-5.0-lite` |
| `size` | `2K` |
| `responseFormat` | `url` |
| `outputFormat` | `png` |
| `watermark` | `false` |
| `webSearch` | `false`；实时新闻、赛事或天气题材需要时再开启 |

Agent Plan 支持文生图、单图生图和多图融合。发送一张或多张原图并附上修改要求时，插件会通过请求体的 `image` 字段提交参考图；支持可公开访问的 URL 和 Base64 数据 URL。Seedream 5.0 Lite 单次最多接收 14 张参考图。

开启 `webSearch` 后，插件会在 Agent Plan 请求中加入 `tools: [{ "type": "web_search" }]`。该能力依赖当前 Agent Plan 套餐和 API Key 权限，可能增加额度消耗；关闭时插件不会发送 `tools` 字段。主接口和备用接口的联网搜索开关彼此独立。

控制台“API 设置 -> 图像生成 API -> 真实生图测试”可以直接使用当前页面草稿测试主接口或备用接口，并支持上传一张图片进行图生图测试。真实测试会调用上游服务，可能消耗额度或产生费用，因此页面会在执行前二次确认。

### SD WebUI

支持启用了 `--api` 的 AUTOMATIC1111 和 Stable Diffusion WebUI Forge。选择“SD WebUI”后填写服务地址、模型检查点、采样器、调度器、采样步数、CFG、宽高、负面提示词和图生图重绘强度；启用了 `--api-auth` 时再填写用户名和密码。

插件分别调用 `/sdapi/v1/txt2img` 和 `/sdapi/v1/img2img`。模型通过单次请求临时覆盖，请求结束后恢复 WebUI 原设置，不会永久切换服务端模型。所有请求固定使用 `batch_size=1` 和 `n_iter=1`，即使上游返回多张图片也只处理第一张；SD WebUI 标准图生图只使用消息中的第一张参考图。

官方接口文档：[火山方舟图片生成 API](https://console.volcengine.com/ark/region:cn-beijing/docs/82379/1541523?lang=zh)。

## 6. HTTP Skills

控制台“插件设置 -> Skills 设置”可以启用或关闭 HTTP Skills。

已整理的方向包括：

- 天气、空气质量、节假日、日历。
- 快递、热榜、汇率、宏观指标。
- 网页解析、URL 状态、IP、DNS、RDAP、ICP备案。
- 论文、OpenAlex、Semantic Scholar。
- 动漫、游戏、航天、国家信息、地震。
- 开发者包信息、GitHub、Stack Overflow、Hacker News。

排查方式：

```text
#查看工具调用
```

如果用户问了天气但没有调用天气 skill，先检查：

- Skill 是否启用。
- 主模型是否支持工具调用。
- AI 对话规则是否要求优先使用结构化 Skills。
- 对应 skill 的接口是否可访问。

## 7. 群管理

群管理默认关闭。推荐通过控制台配置：

```text
http://127.0.0.1:27891/group-management.html
```

### 群管理命令

| 命令 | 说明 |
| --- | --- |
| `#灵晶开启群管理` | 开启本群群管理相关能力 |
| `#灵晶关闭群管理` | 关闭本群群管理相关能力 |
| `#群总结` | 获取本群总结 |
| `#申请头衔 你的头衔` | 提交群头衔申请 |
| `#头衔申请列表` | 查看待审核头衔 |
| `#同意头衔 编号` | 通过并发放头衔 |
| `#拒绝头衔 编号 理由` | 拒绝头衔 |
| `#取消头衔申请` | 取消自己的头衔申请 |

### 默认设置和单群设置

- 控制台支持“默认设置”。
- 某个群没有单独设置时，会继承默认设置。
- 群列表中可以选择哪些群启用。
- 批量开启、批量关闭、预设规则都需要确认。

### 加群申请

支持：

- 加群申请列表。
- 手动同意/拒绝。
- 自动通过。
- 自定义条件，例如 QQ 等级、账号年龄、问题答案、黑名单、风险词。
- 群管理日志记录申请处理结果。

具体可用字段受适配器影响，适配器拿不到的数据不能作为可靠条件。

### 入群欢迎

入群欢迎已经移动到“群管理”分组。

支持：

- 默认欢迎。
- 单群欢迎。
- 文案编辑。
- 欢迎图片。
- AI 入群欢迎。
- AI 接口故障时回退普通欢迎。

旧命令仍可用：

| 命令 | 说明 |
| --- | --- |
| `#设置欢迎文案 文本` | 设置欢迎文案 |
| `#设置欢迎图片` | 携带图片设置欢迎图 |
| `#查看欢迎` | 查看当前欢迎 |
| `#清除欢迎` | 清除欢迎 |

### 内容风控

支持：

- 链接检测。
- 关键词检测。
- 重复消息检测。
- 刷屏检测。
- 新成员观察期。
- 警告累计。
- 自动撤回、禁言、踢出。

危险动作会受到全局群管安全开关限制。建议先用 `log` 或 `warn` 观察，再逐步开启撤回/禁言。

### 刷屏检测

刷屏检测默认关闭。开启后可配置：

- 时间窗口，例如 60 秒。
- 同一用户消息阈值，例如超过 8 条触发。
- 触发动作，例如记录、警告、撤回、禁言。

### URL 安全检查

URL 安检默认关闭。开启后流程：

1. 从群消息提取 URL。
2. 白名单命中则跳过。
3. 使用网页转 Markdown 接口读取网页内容。
4. 调用已配置的 AI 服务判断钓鱼、诈骗、恶意下载、灰产引流等风险。
5. 结果写入本地缓存，同一 URL 下次复用。

注意：

- 读取网页前会拒绝 localhost、内网、链路本地等地址。
- 外部网页读取服务是否能阻止内网探测风险，取决于该服务自身实现。
- URL 和风险结论会写入群管理日志。

### 群总结

每日群总结配置在 `config/ai.json` 的 `dailyGroupSummary`，也可以在群管理中配置。

支持：

- 每群每日总结。
- 目标群选择。
- 最少消息数。
- 最大消息数。
- 保留天数。
- 自定义总结要求。
- 手动命令 `#群总结`。

### 群头衔申请

配置文件：`config/groupTitle.json`。

要求：

- `groupTitle` 开关开启。
- Bot 在目标群是群主，才能发放群头衔。

支持：

- 用户提交申请。
- 管理员审核。
- 控制台审核。
- 禁用词。
- 长度限制。
- AI 审核合法头衔并自动发放。
- AI 故障时转人工审核。

## 8. 图片监控

配置文件：`config/imageMonitor.json`。

控制台入口：

```text
http://127.0.0.1:27891/image-monitor-center.html
```

支持：

- 独立视觉模型审核。
- 违规图片记录、告警、撤回。
- 表情包识别。
- 表情包按 `角色/情绪` 保存，例如 `芙宁娜/happy`。
- 本地保存审核图片，避免 QQ 图床过期。
- 图片监控页面使用本地图片预览。
- 清理非表情包图片。
- 只保存某些角色或关键词命中的表情包。
- 图片监控审核日志不展示审核规则预览。

### 本地入库与 MD5 去重

“API 设置 -> 图片监控 -> 本地保存与入库”提供“图片本地入库”总开关，默认关闭：

- 关闭时不保存审核预览图，也不保存表情包原图。
- 首次收到图片时写入 `data/crystelf/image-monitor/md5-index.jsonl`。
- 以后收到相同 MD5 的图片会直接跳过视觉模型检测，Bot 重启后仍然有效。
- MD5 索引只记录图片指纹和首次出现时间，不记录图片地址、群号或用户信息。
- 开启总开关后，“保存审核预览图”和“保存表情包图片”两个细分开关才会生效。

稳定入库规则：

- `memeCharacter` 必须是角色名或核心形象名，例如 `芙宁娜`。
- `memeEmotion` 必须是固定枚举：`happy`、`sad`、`angry`、`confused`、`shy`、`surprised`、`default`。
- 不要把场景、动作、长句写成文件夹名。

数据位置：

```text
data/crystelf/image-monitor/
```

## 9. Bot 插件管理

入口：

```text
http://127.0.0.1:27891/bot-plugins.html
```

能力：

- 扫描 Bot 的 `plugins` 目录。
- 展示 Git 状态、版本、依赖、入口文件、脚本。
- 插件目录搜索、筛选。
- 从插件目录安装插件。
- 删除插件到 `plugins/.crystelf-plugin-trash`。

安全默认：

- 插件安装默认“仅克隆仓库”。
- 用户手动选择安装依赖时，执行 `npm install --ignore-scripts`。
- 不接受直接输入任意仓库安装，只能安装目录中的条目。
- 目标目录名会归一化为安全目录名。
- 删除不是硬删，会移入回收站。
- 当前 crystelf 插件不允许删除。

## 10. 模拟调试

入口：

```text
http://127.0.0.1:27891/qq-simulator.html
```

用途：

- 模拟 QQ 群聊天界面。
- 发送文本、图片、语音、截图。
- 模拟 @、回复、戳一戳、CQ 码、meme。
- 构造加群申请、入群、群成员状态。
- 保存场景和回归测试。
- 批量跑场景。
- 渲染真实 QQ 消息段。
- 防重复发送、超时取消。
- 手机屏幕适配。
- “真实生图”模式可在二次确认后调用当前图像接口执行 `#灵晶改图` 或 `#灵晶融合`，结果会直接显示在模拟聊天区；每次固定返回一张图片。

消息段构造器支持插入：

- @用户。
- 图片。
- 语音。
- 表情。
- 回复。
- CQ 码。
- meme。

模拟调试不会向 QQ 群发送消息，也不会执行真实撤回、禁言、踢人、头衔发放等危险操作。“真实生图”是唯一会调用外部服务的调试模式，仅允许改图和融合命令，执行前必须二次确认，批量回放不会触发真实生图。

## 11. 帮助系统

命令：

```text
#灵晶帮助
#灵晶帮助 AI
#灵晶帮助 管理
#灵晶帮助 娱乐
#灵晶帮助 调试
```

控制台入口：

```text
http://127.0.0.1:27891/help-diy.html
```

支持：

- 自定义首页帮助。
- 自定义分类帮助。
- 图片帮助模式。
- 模板。
- 导入导出。
- 历史版本。
- 帮助图片预览。

开启图片模式后，`#灵晶帮助` 会优先发送配置的帮助图片。

## 12. 状态图

命令：

```text
#灵晶状态
```

状态图包含：

- Bot 运行时间。
- 插件版本。
- 系统负载。
- 内存。
- 功能开关。
- AI 请求量和模型用量。
- 生图统计。
- 图片监控审核用量。
- 最近告警。
- 控制台状态。

状态图不会展示控制台 URL。

## 13. 日志排查

总览页提供“日志排查”。

流程：

1. 用户点击“开始排查”。
2. 控制台读取最近 Bot / 插件日志尾部。
3. 提取错误和告警片段。
4. 做基础脱敏。
5. 调用已配置的 AI 服务输出中文排查建议。

注意：

- 日志可能包含群聊内容、路径、接口错误和未匹配的敏感信息。
- 只建议在可信 AI 接口下使用。
- 控制台只读模式或日志暴露关闭时不可用。

## 14. 数据和配置

| 路径 | 说明 |
| --- | --- |
| `config/config.json` | 主功能开关、控制台设置 |
| `config/ai.json` | AI、图像生成、群总结、人格、知识库 |
| `config/imageMonitor.json` | 图片监控 |
| `config/groupTitle.json` | 群头衔申请 |
| `config/skills*.json` | Skills 主开关和索引 |
| `config/skills/` | 内置 Skill 定义 |
| `data/crystelf/` | 运行时数据、缓存、用量、群管理日志 |
| `data/crystelf/image-monitor/` | 图片监控审核图、表情包、审核日志和持久化 MD5 索引 |
| `temp/` | 控制台临时日志、预览文件 |

长期维护建议优先使用控制台或锅巴配置，减少直接改默认 JSON。

## 15. 常见问题

### 控制台打不开

检查：

- Bot 是否已启动。
- `config.webConsole` 是否为 `true`。
- 端口是否是 `27891`。
- 是否端口冲突。
- 是否需要重启 Bot 加载新路由。

### 登录口令不知道

查看或设置：

```text
config/config.json -> webConsoleToken
```

首次本机访问也可以按登录页提示初始化。

### `/api/group-management` 返回 404

通常是 Bot 进程还在跑旧代码。重启 Bot 后再打开群管理页面。

### AI 生图失败或超时

检查：

- `ai.imageConfig.enabled` 是否开启。
- `imageMode` 是否正确。
- `model` 是否支持生图。
- `baseApi` 是否是基地址。
- `apiKey` 是否正确。
- 慢接口把 `timeout` 调高。
- `gpt-image-2` 推荐 `responseFormat=b64_json`。

### 图片监控产生大量奇怪文件夹

检查视觉模型输出：

- `memeCharacter` 是否是稳定角色名。
- `memeEmotion` 是否是固定枚举。
- 是否需要启用角色/关键词保存过滤。
- 是否需要清理非表情包图片。

### 群头衔发放失败

检查：

- `groupTitle` 是否开启。
- Bot 是否是群主。
- 头衔长度是否超限。
- 是否命中禁用词。
- 当前适配器是否支持设置群头衔。

## 16. 本地健康检查

```bash
npm run check
```

完整检查包含编码、结构、配置、前端脚本、核心功能、图片渲染和控制台接口。如果只想快速检查静态文件，可以运行：

```bash
npm run check:quick
```

如果更新后控制台页面或群内命令异常，可以先运行完整检查。更新了控制台页面后，建议重启 Bot 再打开相关页面实测。
